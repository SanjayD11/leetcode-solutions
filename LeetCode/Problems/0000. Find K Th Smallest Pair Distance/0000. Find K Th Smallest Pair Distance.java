        }    }    return ans;    }    private boolean possible(int nums[], int k, int distance){    int count = 0;    int left = 0;        else{            low = mid+1;            high = mid-1;        }        int mid = low+(high-low)/2;        if(possible(nums, k, mid)){            ans = mid;    int ans = 0;    while(low <= high){    int high = nums[n-1]-nums[0];    for(int right = 0; right < nums.length; right++){        while(nums[right]-nums[left] > distance){            left++;        }        count += right-left;    }