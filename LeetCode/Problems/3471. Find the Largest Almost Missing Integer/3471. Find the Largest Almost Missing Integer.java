            ans = Math.max(ans, num);        }        for(int num : nums){        int ans = -1;    if(k==n){        return ans;    }        }            }            ans = Math.max(ans, num);            if(map.get(num) == 1){        for(int num : nums){        int ans = -1;    if(k==1){    }        map.put(nums[i], map.getOrDefault(nums[i], 0)+1);    for(int i = 0; i < n; i++){    HashMap<Integer, Integer> map = new HashMap<>();    int n = nums.length;    public int largestInteger(int[] nums, int k) {class Solution {// HashMap Approach :