            high = mid;        }    }       return low;    }}/*//idhu oru boundary binary search. Peak index find panrom, exact target search illa            low = mid+1;        }        else{        int mid = low + (high-low)/2;        if(arr[mid] < arr[mid+1]){    int high = arr.length-1;    while(low < high){    public int peakIndexInMountainArray(int[] arr) {    int low = 0;class Solution {