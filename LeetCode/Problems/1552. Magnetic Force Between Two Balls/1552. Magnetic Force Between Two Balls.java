        int balls = 1;    }    private boolean canPlace(int[] position, int m, int distance){    }    return ans;            else{            high = mid-1;        }            low = mid+1;        }        if(canPlace(position, m, mid)){            ans = mid;    while(low <= high){        int mid = low + (high-low)/2;    int high = position[position.length-1] - position[0];    int ans = 1;    public int maxDistance(int[] position, int m) {    Arrays.sort(position);    int low = 1;class Solution {