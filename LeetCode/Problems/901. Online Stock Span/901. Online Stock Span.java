    }}/** * Your StockSpanner object will be instantiated and called as such: * StockSpanner obj = new StockSpanner(); * int param_1 = obj.next(price);        st.push(new int[]{price, span});        return span;            span += st.pop()[1];        }    public int next(int price) {        int span = 1;        while(!st.isEmpty() && st.peek()[0] <= price){        st = new Stack<>();    }    Stack<int[]> st;    public StockSpanner() {// monotonic decreasing stack approach :class StockSpanner {