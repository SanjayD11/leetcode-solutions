        int col = 0;        int row = n-1;        int n = matrix.length;    private int countLessEqual(int[][] matrix, int k, int target){    }    return ans;    }        }            low = mid+1;        else{        }            high = mid-1;            ans = mid;        if(countLessEqual(matrix, k, mid) >= k){        int mid = low + (high-low)/2;    while(low <= high){    int ans = low;    int high = matrix[n-1][n-1];    int low = matrix[0][0];    public int kthSmallest(int[][] matrix, int k) {    int n = matrix.length;class Solution {//Binary Search Approach :