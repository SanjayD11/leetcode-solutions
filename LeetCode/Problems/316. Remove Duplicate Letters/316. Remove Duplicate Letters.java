        st.push(c);        }            used[removed-'a'] = false;            char removed = st.pop();        while(!st.isEmpty() && st.peek() > c && lastIndex[st.peek()-'a']>i){        }            continue;        if(used[c-'a']){        char c = s.charAt(i);    for(int i = 0; i < n; i++){    }        lastIndex[s.charAt(i)-'a'] = i;    for(int i = 0; i < n; i++){    int n = s.length();    Stack<Character> st = new Stack<>();    int lastIndex[] = new int[26];    boolean[] used = new boolean[26];    public String removeDuplicateLetters(String s) {class Solution {