    return ans;    }    private boolean possible(int price[], int k, int tastiness){        int candy = 1;        int last = price[0];        for(int i = 1; i < price.length; i++){            if(price[i] - last >= tastiness){                candy++;                last = price[i];            }        }        return candy >= k;    }}// Price array ah sort pannrom, so smallest difference adjacent selected candies kulla dhaan varum.// Tastiness (mid) nu minimum gap ah assume panni check pannrom.// Difference >= mid irundha greedily next candy pick pannrom.// k candies pick panna mudinja bigger tastiness try pannrom, illa na smaller tastiness try pannrom.