//         for (int capacity = maxWeight; capacity <= totalWeight; capacity++) {//             int currentLoad = 0;//             int requiredDays = 1;//             for (int weight : weights) {//                 if (currentLoad + weight <= capacity) {//                     currentLoad += weight;//                 } else {//                     requiredDays++;//                     currentLoad = weight;//                 }//             }//             if (requiredDays <= days) {//                 return capacity;//             }//         }//             totalWeight += weight;//             maxWeight = Math.max(maxWeight, weight);//         for (int weight : weights) {//         }//         return totalWeight;//     }// }