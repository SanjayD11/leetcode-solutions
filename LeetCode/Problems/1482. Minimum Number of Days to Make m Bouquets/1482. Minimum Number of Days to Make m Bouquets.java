//             for (int bloom : bloomDay) {//             int flowers = 0;//             int bouquets = 0;//         }//         for (int day = minDay; day <= maxDay; day++) {//             minDay = Math.min(minDay, day);//             maxDay = Math.max(maxDay, day);//         int minDay = Integer.MAX_VALUE;//         int maxDay = Integer.MIN_VALUE;//         for (int day : bloomDay) {//             return -1;//         }//     public int minDays(int[] bloomDay, int m, int k) {//         if ((long)m * k > bloomDay.length) {// BruteForce Approach :// class Solution {search panrom.// Bloomed flowers count panni k adjacent flowers aana oru bouquet create panrom.// Bouquets enough na smaller day search panrom, illa na bigger day 