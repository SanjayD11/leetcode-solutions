class Solution {    public int minMoves(int[] nums) {    int min = nums[0];    for(int num : nums){        min = Math.min(min, num);    }        int moves = 0;    for(int num : nums){        moves += num - min;    }    return moves;    }}// Oru move la (n-1) elements ah +1 increase panrom.// Idhu same as remaining 1 element ah -1 decrease panradhu.// Adhanala ella elements um minimum value ku equal aakanum.// Ovvoru element oda (num - min) difference ah add pannina total moves kedaikum.