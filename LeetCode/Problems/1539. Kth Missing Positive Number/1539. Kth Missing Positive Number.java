        int missing = arr[mid] - (mid+1);        if(missing < k){            low = mid+1;        }        else{            high = mid-1;        }    }    return low+k;    }}/*1. Ovvoru index varaikkum ethana missing numbers irukku nu calculate pannrom.2. Missing count formula = arr[mid] - (mid + 1).3. Missing count k vida kammi na kth missing right side la irukkum, so low = mid + 1.4. Missing count k reach pannumbodhu answer position kandupidichiduvom, final answer = low + k.*/