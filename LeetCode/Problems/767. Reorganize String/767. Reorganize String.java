        map.put(second, map.get(second)-1);        map.put(first, map.get(first)-1);        sb.append(second);        sb.append(first);        char second = pq.poll();        char first = pq.poll();    while(pq.size() > 1){    StringBuilder sb = new StringBuilder();    pq.addAll(map.keySet());    get(a));        if(map.get(first) > 0){            pq.offer(first);        }        if(map.get(second) > 0){            pq.offer(second);        }    }    if(!pq.isEmpty()){        char last = pq.poll();        if(map.get(last) > 1){            return ;        }