Increasing stack la bar indices store panrom./*    }}    }       return maxArea;            maxArea = Math.max(maxArea, area);        }        st.push(i);            int width = st.isEmpty() ? i  : i - st.peek() - 1;            int area = heights[top] * width;        while(!st.isEmpty() && curr < heights[st.peek()]){            int top = st.pop();    for(int i = 0; i <= n; i++){        int curr = (i==n) ? 0 : heights[i];    int n = heights.length;    int maxArea = 0;    Stack<Integer> st = new Stack<>();class Solution {    public int largestRectangleArea(int[] heights) {