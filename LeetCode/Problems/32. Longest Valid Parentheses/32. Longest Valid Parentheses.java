//         }//                 open = close = 0;//             }//             if (open == close) {//                 max = Math.max(max, open + close);//             } else if (close > open) {//             else close++;//             if (c == '(') open++;//             char c = s.charAt(i);//         for (int i = 0; i < s.length(); i++) {// class Solution {//     public int longestValidParentheses(String s) {//         int open = 0, close = 0, max = 0;//         open = close = 0;//         for (int i = s.length() - 1; i >= 0; i--) {//             char c = s.charAt(i);//             if (c == '(') open++;