//             int minIndex = start;//             //Smallest element inside valid range la find pannu//             for (int i = start; i <= n - (k - pos); i++) {//                 if (nums[i] < nums[minIndex]) {//         for (int pos = 0; pos < k; pos++) {//         int start = 0;//         int[] ans = new int[k];//         int n = nums.length;//                     minIndex = i;//                 }//             }//             ans[pos] = nums[minIndex];//             start = minIndex + 1;//         }//         return ans;//     }// }