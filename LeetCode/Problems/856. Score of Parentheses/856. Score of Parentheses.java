            while(st.peek() != 0){                val += st.pop();            }            val = Math.max(2*val, 1);            st.pop();            st.push(val);        }    }    while(!st.isEmpty()){        score += st.pop();    }    return score;    }}/* 1. '(' vandha new bracket level start nu marker ah 0 push pannrom. 2. ')' vandha marker (0) varaikum ullla scores ellam add panni, () na 1 illa  (A) na 2 * score calculate pannrom. 3. Marker remove panni new score ah stack la push pannrom; last la stack  scores ellam add pannina final answer. */