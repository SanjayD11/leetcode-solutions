//         while(!queue.isEmpty()){//         map.put(node, clone);//         queue.offer(node);//         Queue<Node> queue = new LinkedList<>();//         Node clone = new Node(node.val);//             return null;//         }//         HashMap<Node, Node> map = new HashMap<>();//     public Node cloneGraph(Node node) {//         if(node == null){// BFS Approach :// class Solution {*/la connect pannrom.Already cloned node vandha HashMap la irundhu direct clone return panni cycle avoid pannrom.save pannrom.DFS use panni ovvoru neighbor ah clone panni current clone oda neighbors /*Current node ku new clone create panni original-clone mapping HashMap la     }}