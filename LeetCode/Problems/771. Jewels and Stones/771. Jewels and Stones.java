//         map.put(c, map.getOrDefault(c, 0)+1);//     for(char c : stones.toCharArray()){//     HashMap<Character, Integer> map = new HashMap<>();//     int ans = 0;//     public int numJewelsInStones(String jewels, String stones) {// class Solution {// HashMap Approach :}    }        return ans;        }            }                ans++;            if(set.contains(c)){        for(char c : stones.toCharArray()){        int ans = 0;        }            set.add(c);        for(char c : jewels.toCharArray()){        HashSet<Character> set = new HashSet<>();    public int numJewelsInStones(String jewels, String stones) {class Solution {// HashSet Approach :