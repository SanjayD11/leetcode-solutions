        st.push(i);    }    long ans = 0;    for(int i = 0; i < n; i++){        ans = Math.max(ans, (left[i]+right[i])-heights[i]);    }    return ans;    }}/*1. Monotonic Increasing Stack use panni Previous Smaller & Next Smaller index find pannrom.2. left[i] = left side la maximum mountain sum calculate pannrom.3. right[i] = right side la maximum mountain sum calculate pannrom.4. Current height, nearest smaller tower varaikum mattum extend aagum.5. ans = left[i] + right[i] - heights[i] (peak rendu thadava count aaguradhu nala once subtract pannrom).*/