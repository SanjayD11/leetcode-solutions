    }}    return result;            low = mid+1;        }    }             }        else{            high = mid-1;// mid is already stored so safe to remove and             go left        if(letters[mid] > target){            result = letters[mid];    while(low <= high){        int mid = low + (high-low)/2;    int low = 0;    int high = letters.length-1;    char result = letters[0];class Solution {    public char nextGreatestLetter(char[] letters, char target) {