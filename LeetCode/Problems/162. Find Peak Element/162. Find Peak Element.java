    public int findPeakElement(int[] nums) {        int low = 0;        int high = nums.length-1;        while(low < high){ //low <= high use panna high = mid nala infinite loop         varum.            int mid = low + (high-low)/2;            if(nums[mid] < nums[mid+1]){                low = mid+1;            }            else{            high = mid;   //            // nums[mid] > nums[mid+1], Downhill-ku             vandhutom. Peak mid la irukkalam illa left side-la irukkalam.So mid ah             remove panna koodadhu. Search space ah [low to mid] nu reduce             pannrom                                                     }        }        return low; // when low == high loop ends, and this will be the peak         element    }}class Solution {